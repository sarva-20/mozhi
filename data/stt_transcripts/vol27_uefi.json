{
  "file": "vol27_uefi.mp3",
  "transcript": "I think the outrage was caused by the miss configured load balance at home. The ticket got autocrossed before anyone actually dissolved it. The health risk was backed up because of the outrage earlier. I restarted the service but the queue was still packed. I restarted, the client keeps hitting blogged out every 15 minutes for no reason. When we get someone from the networking on the call, it's their subnet. The backup restore is signed but the indexes need to be revealed. We roll back the releases because the checkout flow was thrown at us. The report generator hangs out whenever the data ranges too wide. We should stagger the update rollout instead of pushing it everywhere at once. Someone needs to double check the retention policy before we delete an event. The security team flagged the possible SQL injection attempt.",
  "audio_duration_s": 57.877,
  "transcription_time_s": 2.433,
  "real_time_factor": 0.042,
  "detected_language": "en"
}
