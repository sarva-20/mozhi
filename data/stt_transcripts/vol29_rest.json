{
  "file": "vol29_rest.mp3",
  "transcript": "The change request means signing off before it goes to the review board. The van link to the branch office keeps dropping every afternoon. Can we schedule a maintenance window for the database migration? The work around is fine for now. We should fix the roof cost eventually. The test environment doesn't have the same data volume as protection. We are still waiting on the vendor to confirm the maintenance window. Can you check whether the lotus numbers actually match the production traffic? The shared inbox holds code overwritten during the mail migration. The end point returns a 500 error whenever the request order is empty. The proxy's catching stale responses even after weekly order. The deployment pipeline is still stuck waiting for a manual approval step. The new intent is accidentally forced over the main branch.",
  "audio_duration_s": 59.349,
  "transcription_time_s": 2.199,
  "real_time_factor": 0.037,
  "detected_language": "en"
}
